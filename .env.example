# Local development MongoDB single-node replica set. Do not use production data here.
# Configure the MongoDB service as documented in migration-docs/06-transaction-capable-development-mongodb.md.
MONGODB_URI=mongodb://127.0.0.1:27017/warehouse_system_next?replicaSet=erpDevRs

# Replace with a newly generated local development secret.
JWT_SECRET=replace_with_a_new_local_development_secret
JWT_EXPIRES_IN=7d
